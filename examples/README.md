# Examples

This directory will contain human-readable examples of the x402 protocol:

- `challenge-example.json` — example challenge object
- `proof-example.json` — example proof object
- `http-flow.md` — example HTTP request/response flow
